[package]
name = "block-io-viz-common"
version = "0.1.0"
edition = "2021"

[features]
default = []
user = ["serde", "aya"]

[dependencies]
aya = { git = "https://github.com/aya-rs/aya", optional = true }
serde = {version = "1.*", features = ["derive"], optional = true}

[lib]
path = "src/lib.rs"
